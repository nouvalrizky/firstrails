<div class="card mb-3">
    <div class="card-header">
        <div class="d-flex align-items-center">
            <div class="me-auto">
                <% if published_tweet.published? %>
                <span class="badge bg-primary mb-2">Published</span>
                <% else %>
                <span class="badge bg-secondary mb-2">Unpublished</span>
                <% end %>
                <p class="fw-bold">
                    Scheduled for <%= l published_tweet.publish_at, format: :long %>
                </p>
            </div>
            <div>
                <%= image_tag published_tweet.twitter_account.image, class: "rounded-circle" %>
            </div>
            <div>
                <%= link_to  "#{published_tweet.twitter_account.username}", "https://twitter.com/#{published_tweet.twitter_account.username}", class: "nav-link", target: :_blank %>
            </div>
        </div>
    </div>
    <div class="card-body">
        <p><%= published_tweet.body %></p>
        <% if published_tweet.published? %>  
            <%= link_to  "View Tweet", "https://twitter.com/#{published_tweet.twitter_account.username}/status/#{published_tweet.tweet_id}", class: "text-decoration-none" %>
        <% end %>      
    </div>
</div>
